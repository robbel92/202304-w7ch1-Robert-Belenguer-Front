sonar.projectKey=caminolosada_202304-w6chwe-Camino-Losada-Front
sonar.organization=caminolosada

# Encoding of the source code. Default is default system encoding
#sonar.sourceEncoding=UTF-8

# Path is relative to the sonar-project.properties file. Replace "\" by "/" on Windows.
sonar.sources=./src

# Encoding of the source code. Default is default system encoding
sonar.sourceEncoding=UTF-8

sonar.test.inclusions=src/**/*.test.ts, src/**/*.test.tsx
sonar.coverage.exclusions=src/index.tsx, src/styles/GlobalStyle.ts, src/styles/styled.d.ts, src/routers/appRouter.tsx, src/setupTests.ts

sonar.javascript.lcov.reportPaths=coverage/lcov.info
